const nextConfig = {
  images: {
    domains: [
      "fakestoreapi.com",
      "images.unsplash.com",
      "cdn.dummyjson.com",
      "via.placeholder.com"
    ],
  },
};

export default nextConfig;